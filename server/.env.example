# 허용할 클라이언트 origin (콤마로 구분). 지정하지 않으면 기본값(배포 주소 + localhost:5173) 사용
# CLIENT_ORIGIN=https://daenamu-soop.vercel.app,http://localhost:5173

# 메시지 신고 시 알림을 받을 Discord 웹훅 URL. 채널 설정 > 연동 > 웹후크에서 생성.
# 지정하지 않으면 신고 기능은 조용히 무시됨(서버가 죽지 않음).
# REPORT_WEBHOOK_URL=https://discord.com/api/webhooks/xxxx/yyyy
